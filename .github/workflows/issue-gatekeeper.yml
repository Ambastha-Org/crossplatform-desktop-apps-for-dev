name: "🛡️ Issue Gatekeeper"

on:
  issues:
    types: [opened, edited, reopened]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  setup:
    name: "🧠 Unified Parsing"
    runs-on: ubuntu-latest
    outputs:
      win_id: ${{ steps.parse.outputs.win_id }}
      brew_id: ${{ steps.parse.outputs.brew_id }}
      tool_name: ${{ steps.parse.outputs.tool_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Ensure Labels Exist
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const labels = ['🚫 duplicate','✅ auto-verified','🔥 confirmed-broken','🧪 needs-verification','✨ new-tool','🚨 bug','⚡ priority'];
            for (const name of labels) {
              try {
                await github.rest.issues.getLabel({ owner, repo, name });
              } catch (e) {
                await github.rest.issues.createLabel({ owner, repo, name, color: 'ededed' });
              }
            }

      - name: Parse YAML & Check Duplicates
        id: parse
        uses: actions/github-script@v7
        with:
          script: |
            const script = require('./scripts/issue_aggregator.js');
            await script({github, context, core});
            core.setOutput('win_id', process.env.REQ_WIN_ID || '');
            core.setOutput('brew_id', process.env.REQ_BREW_ID || '');
            core.setOutput('tool_name', process.env.TOOL_NAME || '');

  verify:
    name: "🔍 Verification"
    needs: setup
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: winget
            script: "scripts/verify_winget.ps1"
          - os: macos-latest
            platform: homebrew
            script: "scripts/verify_brew.sh"
    runs-on: ${{ matrix.os }}
    env:
      REQ_WIN_ID: ${{ needs.setup.outputs.win_id }}
      REQ_BREW_ID: ${{ needs.setup.outputs.brew_id }}
    outputs:
      win_data: ${{ steps.capture.outputs.win_data }}
      brew_data: ${{ steps.capture.outputs.brew_data }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Verification
        id: run_script
        shell: ${{ matrix.os == 'windows-latest' && 'pwsh' || 'bash' }}
        run: |
          if ("${{ matrix.os }}" -eq "windows-latest") {
            ./${{ matrix.script }} > output.txt
          } else {
            chmod +x ./${{ matrix.script }}
            ./${{ matrix.script }} > output.txt
          }
          cat output.txt

      - name: Capture JSON Result
        id: capture
        shell: bash
        run: |
          DATA=$(grep "RESULT_JSON=" output.txt | sed 's/RESULT_JSON=//')
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            echo "win_data=$DATA" >> $GITHUB_OUTPUT
          else
            echo "brew_data=$DATA" >> $GITHUB_OUTPUT
          fi

  aggregate:
    name: "🔗 Process & Respond"
    needs: [setup, verify]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Finalize Issue Status
        uses: actions/github-script@v7
        env:
          WIN_DATA: ${{ needs.verify.outputs.win_data }}
          BREW_DATA: ${{ needs.verify.outputs.brew_data }}
        with:
          script: |
            const script = require('./scripts/issue_aggregator.js');
            await script({github, context, core});

      - name: "🚀 Run PR Logic"
        if: |
          success() && 
          (contains(needs.verify.outputs.win_data, '"found": true') || 
           contains(needs.verify.outputs.brew_data, '"found": true'))
        env:
          WIN_DATA: ${{ needs.verify.outputs.win_data }}
          BREW_DATA: ${{ needs.verify.outputs.brew_data }}
          TOOL_NAME: ${{ needs.setup.outputs.tool_name }}
        run: |
          WIN_SUG=$(echo $WIN_DATA | jq -r '.suggestion // ""')
          BREW_SUG=$(echo $BREW_DATA | jq -r '.suggestion // ""')
          echo "{\"win_id\": \"$WIN_SUG\", \"brew_id\": \"$BREW_SUG\", \"name\": \"$TOOL_NAME\"}" > /tmp/entry.json
          python3 scripts/add_tool_entry.py /tmp/entry.json

      - name: "📤 Create or Update PR"
        if: |
          success() && 
          (contains(needs.verify.outputs.win_data, '"found": true') || 
           contains(needs.verify.outputs.brew_data, '"found": true'))
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: "automated-tool-updates"
          commit-message: "feat: update os_tools.json via Registry-Bot 🤖"
          title: "🚀 [Automated] New Tool Proposals"
          body: "Verified tool requests from Issue #${{ github.event.issue.number }}."
          delete-branch: false
