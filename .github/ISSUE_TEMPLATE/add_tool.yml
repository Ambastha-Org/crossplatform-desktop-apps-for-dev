name: "✨ Add a New Tool"
description: "Request to add a new WinGet or Homebrew tool to the Universal Registry"
title: "🚀 [ADD]: <Tool Name>"
labels: ["✨ enhancement", "🧪 needs-verification"]
body:
  - type: markdown
    attributes:
      value: |
        ### 🆕 New Tool Request
        Use this form to suggest a new tool for the registry.
        Our **Ambastha-Bot** will verify the WinGet/Brew IDs automatically.

  - type: input
    id: tool_name
    attributes:
      label: "📛 Tool Name"
      placeholder: "The name as it appears in the registry"
    validations:
      required: true

  - type: textarea
    id: issue_details
    attributes:
      label: "📝 What is broken?"
      placeholder: "e.g., The WinGet ID has changed to Microsoft.VisualStudioCode"
    validations:
      required: true

  - type: input
    id: winget_id
    attributes:
      label: "🪟 WinGet ID (Windows)"
      placeholder: "Enter the correct WinGet ID"

  - type: input
    id: brew_id
    attributes:
      label: "🍎 Homebrew ID (macOS/Darwin)"
      placeholder: "Enter the correct Homebrew ID"

  - type: markdown
    attributes:
      value: |
        ---
        ### 🤖 Internal Registry Data (Do Not Edit)

  - type: checkboxes
    id: verification
    attributes:
      label: "✅ Verification Step"
      options:
        - label: "I have checked that these IDs are currently valid."
          required: true
