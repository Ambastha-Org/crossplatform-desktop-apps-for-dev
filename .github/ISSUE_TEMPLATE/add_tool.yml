name: "➕ Add a New Tool"
description: "Suggest a cross-platform tool for the universal developer registry"
title: "🚀 [ADD]: <Tool Name>"
labels: ["✨ new-tool", "🧪 needs-verification"]
body:
  - type: markdown
    attributes:
      value: |
        ### 🌟 New Tool Proposal
        Thank you for contributing! Our bot will verify these IDs and check for duplicates.

  - type: input
    id: tool_name
    attributes:
      label: "📛 Tool Name"
      placeholder: "e.g., MongoDB Compass"
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: "📝 Short Description"
      placeholder: "Briefly explain what this tool does."
    validations:
      required: true

  - type: input
    id: winget_id
    attributes:
      label: "🪟 WinGet ID (Windows)"
      placeholder: "e.g., MongoDB.Compass.Full"
    validations:
      required: true

  - type: input
    id: brew_id
    attributes:
      label: "🍎 Homebrew ID (macOS/Darwin)"
      placeholder: "e.g., mongodb-compass"
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ### 🤖 Internal Registry Data (Do Not Edit)
        *The bot will automatically extract data from the fields above.*

  - type: checkboxes
    id: verification
    attributes:
      label: "✅ Verification Step"
      options:
        - label: "I have verified these IDs exist."
          required: true
