name: "🚨 Report a Broken Tool"
description: "Notify us if a WinGet or Brew ID is incorrect or the tool is broken"
title: "🐞 [FIX]: <Tool Name>"
labels: ["🚨 bug", "🧪 needs-verification"]
body:
  - type: markdown
    attributes:
      value: |
        ### 🔧 Tool Maintenance Report
        Use this form to report issues with existing registry entries. 
        **Note:** If you are changing an ID, our bot will verify the new ID.

  - type: input
    id: tool_name
    attributes:
      label: "📛 Tool Name"
      placeholder: "The name as it appears in the registry"
    validations:
      required: true

  - type: textarea
    id: issue_details
    attributes:
      label: "📝 What is broken?"
      placeholder: "e.g., The WinGet ID has changed to Microsoft.VisualStudioCode"
    validations:
      required: true

  - type: input
    id: winget_id
    attributes:
      label: "🪟 WinGet ID (Windows)"
      placeholder: "Enter the correct WinGet ID"

  - type: input
    id: brew_id
    attributes:
      label: "🍎 Homebrew ID (macOS/Darwin)"
      placeholder: "Enter the correct Homebrew ID"

  - type: markdown
    attributes:
      value: |
        ---
        ### 🤖 Internal Registry Data (Do Not Edit)

  - type: checkboxes
    id: verification
    attributes:
      label: "✅ Verification Step"
      options:
        - label: "I have checked that these IDs are currently valid."
          required: true
